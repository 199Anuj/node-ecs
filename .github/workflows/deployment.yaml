name: Node JS Project CI/CD

on:
    push:
        branches:
            - main
    workflow_dispatch:

        
jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: print hello
              run: echo "Hello World"


